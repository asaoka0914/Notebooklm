---
name: book-reader
description: 透過已授權的 notebooklm_tools 或 nlm CLI 操作 NotebookLM，讀取指定筆記本並生成完整涵蓋所有章節、忠於原文的讀書報告（Briefing Document）。當使用者說「幫我整理notebooklm + 筆記名稱」（例如「幫我整理notebooklm AI導論」「幫我整理notebooklm+機器學習筆記」）、或明確提到「book-reader」時，一律觸發此技能。即使使用者只是隨口提到「幫我把 XX 筆記整理成報告」而沒有精確講出「notebooklm」四個字，只要情境明顯是指 NotebookLM 筆記本，也應該觸發。
---

# book-reader

透過本機已授權的 `notebooklm_tools` Python CLI 或 `nlm` MCP CLI，找到使用者指定的 NotebookLM 筆記本，並依照 `讀書報告核心概念.md` 的規則生成一份完整涵蓋所有章節、忠於原文、不摻雜個人觀點的讀書報告。

## 為什麼要照這個流程走

NotebookLM 本本身的報告生成是黑箱——你送進去的 query 決定了它的表現，但它不保證涵蓋所有章節或不加自己的詮釋。這個技能的核心價值不是「呼叫 API」這件事本身（那很簡單），而是**在生成前先建立真實章節基準（Ground Truth），生成後嚴格對照基準核對輸出**。跳過核對步驟、只憑 CLI 回傳「成功」就交差，正是之前在 Agent 協作中吃過的大虧（因生成截斷僅涵蓋半本書，卻被誤判為完成）。

---

## 執行流程

### 1. 解析使用者指定的筆記名稱

從使用者訊息中取出筆記本名稱（例如「幫我整理notebooklm AI導論」→ 名稱是「AI導論」）。如果使用者的講法很模糊、沒辦法確定是哪個筆記本，先列出候選再問，不要用猜的送進下一步。

### 2. 確認 CLI 已授權

執行前先確認 CLI 工具可以正常通訊：

- **優先途徑（Python CLI）**：
  ```bash
  python -c "from notebooklm_tools.cli.main import app; app(['notebook', 'list'])"
  ```
- **備用途徑（MCP CLI / nlm）**：
  若 Python CLI 出現授權過期或套件錯誤，改用 `nlm notebook list` 或 `notebooklm-mcp-cli`。
- **絕對禁止**：嚴禁呼叫未經授權的 `npx notebooklm`。

如果授權失敗（token 過期、未登入等），**立刻停下告知使用者具體錯誤訊息**，不要嘗試用其他方式繞過或假裝成功。

### 3. 比對筆記本名稱 → Notebook ID

拿到 `notebook list` 的結果後，用使用者提供的名稱做比對：
- **完全比對**：優先找名稱完全相符的筆記本。
- **模糊比對**：找不到完全相符時，找包含關鍵字的候選。
- **多筆符合**：如果有超過一個候選，列出候選清單給使用者選。
- **完全找不到**：列出目前有的筆記本名稱給使用者看，請他確認正確名稱。

### 3.5 擷取來源目錄並建立章節基準（Ground Truth Baseline）

在呼叫 NotebookLM 生成報告之前，**必須先取得並紀錄來源文件的完整目錄結構與總章數 $N$**，作為後續防漏核對的唯一真實基準。

#### 取決於來源檔案類型與可用工具：

1. **本地 EPUB 檔案（優先解析）**：
   - 使用 Python 標準庫 `zipfile` + `xml.etree.ElementTree` 解析 `.ncx` 檔中的 `navPoint/navLabel`，或 EPUB 3 的 `nav.xhtml` 頁面。
   - **過濾原則**：採用「黑名單排除法」（排除 `封面`、`書名頁`、`版權頁`、`目錄` 等前導/尾註項目），**切勿以特定 Regex（如硬性匹配「第X章」）白名單過濾**，避免英文標題或無序標題被誤殺。
2. **本地 PDF / TXT / MD 檔案**：
   - 提取文件內建 TOC 目錄標題或以標題階層（`#` / `##`）解析出主要章節清單。
3. **雲端 NotebookLM 來源（無本地檔案時）**：
   - 執行 `nlm source list <notebook_id>` 或用 `nlm notebook describe <notebook_id>` / `nlm notebook query "請列出這份來源文件的完整章節目錄標題"` 獲取來源結構。

#### 記錄 Ground Truth 基準：
```text
Ground Truth 章節基準：
  總章數 N = 21 個主要章節 (+ 前言/結語)
  [1] 前言
  [2] 第 1 章｜...
  ...
  [22] 第 21 章｜...
  [23] 結語
```
*若完全無法取得目錄，必須標註 `UNVERIFIED: 無法建立 Ground Truth 章節基準，後續核對可能無法偵測截斷` 並告知使用者。*

### 4. 載入報告生成規則

讀取 `讀書報告核心概念.md` 的完整內容，作為送給 NotebookLM 的查詢需求（query）。這份規則檔案定義了：忠於原文、完整涵蓋所有章節、篇幅由原文決定、以及輸出格式。

### 5. 指揮 NotebookLM 生成報告

用步驟 3 拿到的 Notebook ID + 步驟 4 的查詢內容，呼叫 CLI 產生「Briefing Document」格式的報告。

### 6. 用真實輸出與 Ground Truth 比對核對

生成完成後，執行以下強制檢核：

1. **Ground Truth 1 對 1 涵蓋度比對（最核心）**：
   - 將報告內文出現的 `## [章節名稱]` 與步驟 3.5 紀錄的 Ground Truth 清單進行對照。
   - **截斷判定**：若報告最終章節序號 $M < N$（如全書 21 章但報告只到第 11 章），判定為**產出截斷失敗**，必須觸發 **步驟 6.5 補全機制**。
   - **禁止事項**：**絕對不得僅憑 NotebookLM 自行輸出的「📋 涵蓋度自我檢查清單」判定通過**，該清單常隨同文本被截斷。
2. **忠實度抽查**：
   - 檢查是否包含「這顯示」、「這意味著」、「值得注意的是」等主觀解讀或未記載的背景知識。
3. **完整性與語言檢查**：
   - 確認內文無中途斷句、無未完成區塊；全程使用繁體中文（專有名詞除外）。

### 6.5 截斷補全機制（當 $M < N$ 時觸發）

若步驟 6 判定發生截斷（$M < N$）：

1. 列出缺失的章節範例：`[M+1, M+2, ..., N]`。
2. 啟動分批針對性 query 補全：
   - 傳送 query：「請依據 `讀書報告核心概念.md` 的格式，專門針對來源檔案中『第 M+1 章 到 第 min(M+5, N) 章』進行重點擷取與報告生成。」
3. 將補全產出的章節內容拼接合併至主報告中。
4. 重複步驟 6 驗證，直到 $M = N$。
5. **防無窮迴圈限制**：補全嘗試次數最多上限 **3 次 (`MAX_RETRY_BATCH = 3`)**。若 3 次後仍無法涵蓋全部章節，停下並明確告知使用者目前已完成的章節範圍與未完成部分。

### 7. 交付結果

將最終報告交付給使用者，並明確說明：
- 全書共有多少章節（$N$），本次報告完整涵蓋之章節狀況。
- 是否觸發過分批補全。
- 是否有任何 UNVERIFIED 或內容偏少需要留意的地方。

---

## Guardrails

- 所有動作都在既有全域工程行為準則之下：不確定的 API/CLI 行為要先查證，查不到就標 `UNVERIFIED`，不能把「看起來合理」當成已驗證。
- 這個技能只做讀取與報告生成，不修改、不刪除使用者 NotebookLM 裡的任何筆記本或來源文件。
- 遇到授權失敗、找不到筆記本、生成內容截斷且補全失敗這幾種情況，一律先如實告知使用者，不要自行用替代方案掩蓋問題。

## 檔案位置

本技能與 `讀書報告核心概念.md` 應放在既有的跨裝置技能共享路徑 `~/.gemini/config/skills/book-reader/` 下（透過 `~/.claude/skills` 的 symlink 同步給 Claude/Antigravity 共用），不要另外在別處建立第二份定義。
